// src/types/index.ts
export interface MetalData {
  id: string;
  name: string;
  karat: string;
  currentPrice: string;
  previousClose: string;
  openPrice: string;
  timestamp: string;
  date: string;
}